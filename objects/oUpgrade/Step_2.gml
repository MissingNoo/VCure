switch (upg[$ "id"]) {
    case Weapons.UrukaNote:{
		if (sprite_index == sMonsterPulse) {
		    image_xscale += oGui.e;
		    image_yscale += oGui.e;
		}
		break;
	}
    default:
        // code here
        break;
}